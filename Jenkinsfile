pipeline {
    agent {
        node {
            label 'labeldockerserver'
        }
    }

    stages {
        stage('Build') {
            steps {
                sh 'mkdir june'
            }
        }
        
    }
}
